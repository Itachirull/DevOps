---
name: install maven and openjdk
  hosts: build
  become: yes

  task:
  - name: install mvn
    apt:
      name: "{{ packages}}"
      state: present
    vars:
      packages:
        - maven
        - openjdk-11-jdk
---